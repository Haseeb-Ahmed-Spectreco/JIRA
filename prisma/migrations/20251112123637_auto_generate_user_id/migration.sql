-- AlterTable
-- Note: For MySQL, @default(uuid()) is handled by Prisma Client at the application level.
-- This migration documents the schema change where DefaultUser.id now has @default(uuid())
-- No database schema changes are required as UUID generation happens in the application layer.

