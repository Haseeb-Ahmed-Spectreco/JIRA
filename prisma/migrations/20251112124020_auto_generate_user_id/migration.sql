-- AlterTable
-- Note: For MySQL, @default(uuid()) is handled by Prisma Client at the application level.
-- The DefaultUser.id field now has @default(uuid()) which means Prisma will automatically
-- generate UUIDs when creating new users. No database schema changes are required as
-- UUID generation happens in the application layer, not at the database level.

-- This is an empty migration as no database changes are needed for MySQL UUID defaults.

